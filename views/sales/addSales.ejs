<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %> 
<body>
    <%- include('../partials/header');%> 
    <main class="mainCreate">
        
        <form class="cajaCreate" id="createSale" action="/sales" method="POST" enctype="multipart/form-data">
            <h3>
                Crear una Promoción:
            </h3>
            <div class="containerCreate">
                <div>
                                          <!-- en nombre cambiaste el id de producto por name, GIL -->
                    <p>
                        <br>
                        <label>Nombre del producto</label>
                        <input class="ingresoCreate" type="text" name="name" id="name"  placeholder="Nombre del producto"
                        value="<%= locals.sales ? sales.name : '' %>"
                            class="<%= locals.errors && errors.name ? 'danger' : '' %>">
                        <span class="error"><%= locals.errors && errors.name ? errors.name.msg : '' %> </span> 
                    </p>
    
                    <p>
                        <label>Descuento</label>
                        <textarea class="ingresoCreate" name="discount" id="discount" placeholder="Descuento"
                        value="<%= locals.sales ? sales.discount: '' %>"
                            class="<%= locals.errors && errors.discount? 'danger' : '' %>"></textarea>
                        <span class="error"><%= locals.errors && errors.discount ? errors.discount.msg : '' %> </span>
                    </p>
                </div>

                <div class="imagenEdit2">
                    <label>Imagen</label>
                    <img class="imagenEdit" name="imageView" src="../../images/products/default.png" alt="" id="">
                </div><br>
               
                <p>
                    <br><br>
                    <label>Agregar una imagen</label>
                    <input class="ingresoCreate fileCreate" type="file" name="imagen" accept="image/x-png,image/gif,image/jpeg"></input>
                    <label class="feedBack"><%= locals.errors && errors.image ? errors.image.msg : '' %> </label><br>
                </p>
                <p>
                    <label>Precio</label>
                    <input class="ingresoCreate" type="number" name="price" id="price" placeholder="Precio"
                    value="<%= locals.sales ? sales.price: '' %>"
                            class="<%= locals.errors && errors.price? 'danger' : '' %>">
                    <span class="error"><%= locals.errors && errors.price ? errors.price.msg : '' %> </span>
                </p>

                <p>
                    <button type="submit" class="ingresoCreate" id="botonIngreso">Crear Promo</button> 
                    <button type="reset" class="ingresoCreate" id="botonReseteo">Borrar Campos</button>
                </p>

            </div>

        </form>
    </main>

    <%- include('../partials/footer') %> 
</body>
</html>